// services/transactionService.ts
import { Transaction } from '../models/Transaction';

export class TransactionService {
  public static generateInsights(transactions: Transaction[], userId: string): any[] {
    // Implementação da lógica de geração de insights
  }

  public static detectSubscriptions(transactions: Transaction[]): any {
    // Implementação da lógica de detecção de assinaturas
  }
}
