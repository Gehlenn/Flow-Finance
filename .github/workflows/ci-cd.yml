name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Run unit tests
        run: npm test

      - name: Run critical coverage
        run: npm run test:coverage:critical

      - name: Run E2E tests
        run: npm run test:e2e

      - name: Build frontend
        run: npm run build

      - name: Build backend
        run: |
          cd backend
          npm ci
          npm run build

      - name: Upload coverage reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          path: |
            coverage/
            backend/coverage/

  # Adicione outros jobs conforme necessário (deploy, etc)
