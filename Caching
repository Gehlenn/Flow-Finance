version: '3'
services:
  api:
    build: ./api
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      CACHE_TYPE: simple
    volumes:
      - cache:/var/cache

volumes:
  cache:
